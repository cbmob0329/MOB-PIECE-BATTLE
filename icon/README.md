# ガチャ中央アイコン

`icon/gacha.png` を同名のPNG画像に差し替えてください。現在は仮のMOBバッジです。
背景透過PNG推奨。縦横比を保って表示します。表示枠はおよそ195×160px。
開発中はページを再読み込み、本番プレビューは `pnpm build` 後に再読み込みしてください。
Viteはiconフォルダをそのままdist/iconへコピーします。
画像が読めない場合はCSSのMOBバッジへフォールバックします。
